#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lowcap;

	for (lowcap = 'a'; low <= 'z'; lowcap++)
		putchar(lowcap);
	for (lowcap = 'A'; low <= 'Z'; lowcap++)
                putchar(lowcap);
	putchar('\n');

	return (0);
}
